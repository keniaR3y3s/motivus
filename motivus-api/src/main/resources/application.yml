spring:
  application:
    name: crn
  web:
    resources:
      add-mappings: false
  datasource:
    url: jdbc:mysql://localhost:3306/motivus?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect